[project]
name = "solution"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TODYAS", email = "bogdansytin@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
solution = "solution:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
